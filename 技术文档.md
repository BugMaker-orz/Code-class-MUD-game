# C++ 程序设计实验：纯文字版地牢 MUD 技术文档

> 课程：C++ 程序设计实验
> 项目：文字版-MUD-纯命令地牢双结局（mud_text）
> 语言：C++17 ｜ 交互方式：纯命令行文字 ｜ 编码：UTF-8 + ANSI 彩色
> 文档版本：2026-09

---

## 1. 项目说明

本项目是一个用 C++17 编写的终端文字地牢游戏（MUD，Multi-User Dungeon 风格的交互，单人游玩）。它没有图形界面，没有地图上走格子，玩家的所有操作都靠输入命令完成，例如 `去 东`、`攻击 史莱姆`、`开宝箱`、`交易`。

项目最初包含二维地图的图形版设计，但图形渲染部分（视野计算、地图绘制、方向键移动）工作量较大，且与"练习 C++ 核心能力"这一目标关系不大，因此在原项目基础上制作了纯文字版：保留战斗、商店、掉落、地牢生成这些核心玩法，把二维地图和方向键整个去掉，交互全部改为命令输入。该版本即 `src/text/` 下的代码。

游戏具备完整的玩法循环：玩家出生在地牢第 1 层起点房，逐层探索 5 层地牢，打怪升级、开宝箱、逛商店，最后击败第 5 层魔王通关。过程中有一个特色机制——玩家可以攻击每层讲故事的向导 NPC，击杀后可获得强力装备和资源，但代价是结局从"勇者胜利"变为"新魔王诞生"，由此形成双结局。

源码规模：26 个源文件，约 2300 行（其中文字版专属的 `TextGame.cpp` 有 1023 行，是项目中最大的一个文件）。

## 2. 构建与运行

项目用 CMake 组织，C++17 标准。

```bash
mkdir build && cd build
cmake ..
cmake --build .
./mud_text            # 随机地图开始游戏
./mud_text 42         # 传一个数字作为随机种子，地图可复现，方便调试和测试
```

| 平台 | 编译器 | 说明 |
|------|--------|------|
| Linux / macOS | g++ / clang++（C++17） | 终端默认 UTF-8，直接运行 |
| Windows | MSVC（CMake 中加 `/utf-8`） | 程序启动时自动切 UTF-8 代码页并打开 ANSI 彩色 |

`CMakeLists.txt` 中只把 `src/text/` 和共用核心层的文件列入 `add_executable`，图形版的 `Renderer / InputManager / FOVSystem / GameLoop` 完全不在编译范围内——用不到的代码不参与构建，文字版从根上就不链接二维渲染相关的任何东西。

## 3. 整体设计思路

程序的分层结构如下：

```
交互层（文字版专属）
  TextGame：命令解析 + 文本渲染 + 全部玩法逻辑
  main_text：控制台初始化 + 程序入口
系统层（共用）
  CombatSystem（战斗结算）  DropSystem（掉落）  ShopSystem（商店）
实体层（共用）
  Player  Monster  Npc  Item   ← 都继承自 Entity
地图层（共用）
  Map（瓦片 + 房间）  DungeonGenerator（地牢生成）
基础层（共用）
  Position  TileType  GameContext（全局状态）
```

`TextGame` 是整个游戏唯一的主控类：持有全部游戏状态，负责"渲染一帧画面 → 读一行命令 → 执行 → 再渲染"这一循环。战斗、掉落、商店等具体逻辑被拆成独立的静态函数系统（`CombatSystem`、`DropSystem`），它们不持有任何状态，输入实体和上下文，返回结果。这样 `TextGame.cpp` 虽然体量较大，但每一块逻辑都可以单独查看、单独测试。

所有玩法相关的状态（当前地图、玩家、怪物、NPC、地面物品、状态机、层数）统一放在 `GameContext` 中。`TextGame` 只负责持有并读写它，并不拥有其生命周期——`cleanup()` 负责释放其中所有动态分配的对象。这种"状态集中存放"的设计，使后续添加功能（如攻击向导的血量表）时无需到处修改成员变量。

## 4. 游戏主循环：run()

`run()` 是整个游戏的运行核心：

```cpp
void TextGame::run() {
    newGame();                      // 开局：初始化 + 生成第 1 层
    std::string line;
    while (m_running) {
        if (m_ctx.state == Victory) { /* 按是否背叛显示两种结局 */ }
        if (m_ctx.state == Dead)    { /* 显示死亡画面，N 重开 */ }
        renderFrame();              // ① 打印一帧画面（小地图→房间→信息→状态）
        if (m_trading) renderTrade(std::cout);   // ② 商店子界面
        std::cout << "请输入指令 > ";
        std::cout.flush();          // ③ 立即刷新，不等换行
        std::getline(std::cin, line);  // ④ 读一整行命令
        handleCommand(line);        // ⑤ 执行命令
    }
    cleanup();                      // 收尾：释放所有内存
}
```

交互方式采用"整行命令"，而非图形版的单键移动（按 W 走一格）。这是传统 MUD 的核心体验：游戏进程在 `getline` 处阻塞等待，玩家敲完回车才继续，每次交互都是"画面 → 命令 → 画面 → 命令"的节奏。`std::cout.flush()` 在交互模式下是必要的，否则提示符可能滞留在缓冲区中无法及时输出。

游戏结束后通过 `cleanup()` 统一释放所有动态内存。该函数设计为可重复调用（内部先判断指针再 delete、置空），这样 `newGame()` 重启时可以先行 `cleanup()` 再初始化，不会出现二次释放。

## 5. 渲染管线：一帧画面的拼装

每次玩家输入命令后，整个画面会重新打印一遍。一帧固定分为四段：**本层小地图 → 当前房间描述 → 信息日志 → 状态栏**，每段对应一个函数：

```cpp
void TextGame::renderFrame() {
    // 彩色分隔线 + 层标题
    renderMiniMap(std::cout);       // 段 1：本层房间拓扑小地图
    renderRoomDesc(std::cout);      // 段 2：当前房间文字描述
    renderLog(std::cout);           // 段 3：信息日志（最近 15 条）
    renderStatus(std::cout);        // 段 4：状态栏
    std::cout.flush();
}
```

### 5.1 文字小地图

这是整个程序中"图形化"程度最高的部分，但完全不用画布，纯字符串拼接。算法分三步：

1. **确定网格尺寸**：遍历所有房间，取最大的 `gridX` / `gridY`，得到小地图的列数和行数。
2. **建字符画布**：一个 `rows*2-1` 行、`cols*4-1` 列的字符串数组，先全填空格。×4 和 ×2 的由来：每个房间占 3 个字符宽（`[X]`）加 1 个连线格（`-`），即 4 列；每行房间占 1 行字符加 1 行纵向连线格，即 2 行。
3. **填节点和连线**：每个房间在画布对应位置写 `[X]`，当前房间写 `@` 并上色，其他按类型写 `S/F/T/$/>/B`。连线不做无脑绘制——横向检查右边邻居是否真的在 `connectedRooms` 中，纵向检查下边邻居，只有真实连接才画 `-` 或 `|`。

绘制效果：

```
[@]-[F]-[F]
 |   |   |
[T]-[>]-[$]
```

### 5.2 房间描述

`renderRoomDesc()` 按固定顺序拼装：房间名（黄色）→ 房间内的怪物（红字名 + HP/攻/防 + 战斗提示）→ 地面物品 → NPC（商人给交易提示，向导给对话提示 + 鲜红的"攻击向导"警告）→ 楼梯 → 出口。

出口方向的判定利用房间网格坐标差：

```cpp
if (nr.gridX == cr.gridX + 1) dir = "东";
else if (nr.gridX == cr.gridX - 1) dir = "西";
else if (nr.gridY == cr.gridY + 1) dir = "南";
else if (nr.gridY == cr.gridY - 1) dir = "北";
```

这段逻辑在进房间写日志的 `logRoomDescription()` 中还有一份完全相同的实现——两处都需要显示出口，代码因此重复。这是因为两处输出目标不同（一个直接打印、一个写日志），完全可以把公共部分抽成一个返回字符串的函数，属于已知的改进点，暂时保留。

## 6. 命令分发：handleCommand

这是整个文字版的"大脑"，一个函数中用 `if / else if` 链把命令分发给各个动作。流程是：先把输入 `trim` 去空白、`toLower` 转小写，然后从上到下匹配：方向、攻击、逃跑、开宝箱、对话、交易、拾取、背包、使用、下楼、等待、查看、历史、帮助、退出。

有两个实现细节值得注意。

**第一，中文参数的"字节安全"裁剪。** 中文在 UTF-8 中一个汉字占 3 字节。最初采用"按固定字节数"的方式取参数（如 `line.substr(2)`），结果 `买 1` 会从"买"的第 3 个字节开始切，切出半个汉字导致解析失败。后来改为用 `line.find(token)` 找到 token 的实际位置和字节长度再切，问题才彻底解决。`argAfter()` 正是为解决该 bug 而引入的：

```cpp
static std::string argAfter(const std::string& line, const std::string& token) {
    size_t p = line.find(token);
    if (p == std::string::npos) return "";
    return line.substr(p + token.size());
}
```

**第二，`查看` 命令的实现是空的。** `{ return; }`——因为主循环每轮都会自动重渲染，`查看` 本身是"免费"的，保留一个命令入口只是为了符合玩家直觉。

## 7. 几个值得注意的设计

### 7.1 双结局：一个布尔标记

游戏的核心特色是双结局，实现上非常朴素——`TextGame` 中有一个 `bool m_betrayedGuide`，贯穿整局游戏：

```
newGame()  →  m_betrayedGuide = false      开局：清白
攻击并击杀向导 →  m_betrayedGuide = true   背叛：深渊开始注视你
击败魔王（tryAttack 的 Boss 分支）→
     if (m_betrayedGuide)  →  "新魔王诞生"结局
     else                  →  "胜利"结局
```

这里刻意选择了"标记 + 终局判定"而非"击杀向导当场变魔王"。因为需求是"**当你击败魔王后**会成为新的魔王"——背叛的后果要留到终局才兑现。击杀向导时只打标记、给出红色背叛警告（玩家可以后悔，但无法撤销），悬念留到击败魔王那一刻。该设计使"背叛"具备完整的过程，而非即时的状态切换。

### 7.2 攻击向导：血量为何存在 map 中

攻击向导是"逐回合"战斗：玩家输入一次 `攻击 向导` 只打一下，向导未死则等待下一次命令。两次命令之间，向导的血量需要被持久记录。实现采用 `std::map<Npc*, int> m_guideHp`，以向导对象指针为 key、剩余血量为 value：

```cpp
int maxHp = 40 + m_ctx.currentLevel * 15;
auto it = m_guideHp.find(guide);
int hp = (it == m_guideHp.end()) ? maxHp : it->second;
// ... 计算伤害，扣血 ...
m_guideHp[guide] = hp;      // 存回血量表，跨回合持久
```

使用 map 而非在 `Npc` 上直接加成员变量，原因在于：每层向导是 `new` 出来的不同对象，`map` 天然按对象分开记账；换层时 `clearLevelEntities()` 会清掉旧层所有实体，向导血量表也随之清空，用 map 一次性 `clear()` 即可，无需逐个对象处理。

数值上也设计了风险：第 1 层向导 55 血，玩家攻 6，需打 8~10 回合；向导反击伤害为 `3+层`，玩家 36 血裸打会死。因此想背刺向导，须先买药水、打怪升级——高风险高回报，使"背叛"成为一个需要认真权衡的决策。

### 7.3 中文乱码与 ANSI 彩色

Windows 控制台默认代码页是 GBK(CP936)，而源码和输出均为 UTF-8，直接打印必然乱码。`main_text.cpp` 的 `setupConsole()` 专门处理该问题，共五步：

1. `SetConsoleOutputCP(CP_UTF8)` / `SetConsoleCP(CP_UTF8)`：将输入输出代码页切换至 UTF-8
2. `_setmode(..., _O_BINARY)`：stdout/stderr/stdin 切二进制模式，避免 C 运行时在 UTF-8 字节上再做一次 GBK 转码
3. `setlocale(LC_ALL, ".UTF-8")`：使 `isalpha` 等函数按 UTF-8 理解字节
4. 打开 `ENABLE_VIRTUAL_TERMINAL_PROCESSING`：这是 ANSI 彩色在 cmd 中生效的开关，不打开时 `\033[91m` 等颜色码会以 `←[91m` 的形式原样显示

Linux/macOS 无需任何处理，终端天然 UTF-8。这也是该游戏在 Windows 上需要专门编写大量 `#ifdef _WIN32`、而在 Linux 上代码量骤减的原因。

彩色系统本身是对 ANSI 转义序列的封装：

```cpp
std::string TextGame::col(const std::string& code, const std::string& text) {
    return code + text + C_RESET;   // 颜色码 + 文本 + 复位码
}
```

用色遵循一套固定约定：**正文白色、区块标题彩色、危险与敌意鲜红**。金币用橙色、玩家定位用蓝色、房间名和 NPC 名字用黄色。这样满屏白字中一眼即可分出层次，无需细读内容就知道哪里是危险、哪里是资源。

### 7.4 地牢生成：每层布局变化

`DungeonGenerator` 负责生成一层地牢，共五步：

1. 按层选布局：奇数层 3×2 横排，偶数层（非末层）2×3 竖排——保证每层布局有变化
2. 计算总尺寸：`W = cols×13 + (cols-1)×1`，`H = rows×9 + (rows-1)×1`
3. 逐房间挖地板（13×9 含墙，内部 11×7）
4. 向右/向下连接邻居并挖 3 格门，保证全连通、每房最多 4 邻
5. `rooms[0]`（左上角）固定为起点，其余洗牌标类型：非末层放楼梯/商店/宝箱，末层放魔王/商店/宝箱，剩余均为战斗房

生成完地图后，`generateLevel()` 按房间类型铺实体：起点房放本层向导（每层名字和台词不同）、商店房放商人、宝箱房放金币和装备、魔王房放 Boss、楼梯房放楼梯瓦片加一只哥布林守门、战斗房放 1~2 只随机怪。

铺实体时使用 `findFreeTileIn()` 函数寻找房间内的"空地"——不能占楼梯、不能占门、不能与其他实体重叠、不能占玩家出生格。该函数遍历房间矩形，跳过所有不可走格子和已占用格子，返回第一个空位。

## 8. 战斗、掉落与商店

### 8.1 回合制战斗

战斗系统为纯静态实现，一次调用即一回合：

```cpp
// 玩家先手：命中判定 + 伤害 + 击杀结算
CombatResult r = CombatSystem::playerAttackMonster(*m_ctx.player, *pick, m_ctx);
if (!r.targetKilled) {
    // 未击杀，怪物反击一回合
    CombatResult rr = CombatSystem::monsterAttackPlayer(...);
}
```

命中率与伤害公式：

- 命中率：`clamp(70 + 攻×2 - 防×2, 20, 95)%`
- 伤害：`max(攻 - 防/2, 1) + rand(0..2)`（保底 1 点伤害）

击杀怪物后：经验/金币入账、生成掉落物、从怪物列表移除并 `delete`。

### 8.2 掉落表

每类怪物有独立的掉落表，按概率和数量区间生成：

| 怪物 | 掉落 |
|------|------|
| 哥布林 | 金币 60%（1~4）、治疗药水 20% |
| 骷髅 | 金币 80%（2~6）、锈铁剑 20%、旧皮甲 10% |
| 蝙蝠 | 金币 40%（1~2） |
| 史莱姆 | 金币 30%、治疗药水 35%、野果 20% |
| 兽人 | 金币 90%（5~12）、兽人战斧 30%、兽皮甲 25% |
| 魔王 | 金币 100%（25~60）、大治疗药水 90%、魔法剑 60%、秘银铠甲 50% |

掉落物生成后进入地面或背包：金币直接入账，装备进背包。

### 8.3 商店

商店是一个"子界面"：进入商店后 `m_trading = true`，此时 `handleCommand` 顶部分支会截胡 `买 N` / `卖 N` / `离开`，不进入正常命令匹配。定价规则直接明确：买入 = 价值×2，卖出 = 价值÷2。

## 9. 数据与数值设计

| 项 | 值 |
|----|----|
| 层数 / 每层房间数 | 5 / 6 |
| 房间尺寸（含墙） | 13 × 9（内部 11×7） |
| 每层布局 | 层1/3/5 横排 3×2；层2/4 竖排 2×3 |
| 玩家初始 | HP 36/36，攻 6，防 3，金币 0，等级 1 |
| 升级 | 经验 = 等级×100；升级 +5HP、+1攻、+1防、满血 |
| 背包上限 | 20 |
| 命中率 | clamp(70+攻×2−防×2, 20, 95)% |
| 伤害 | max(攻−防/2,1) + rand(0..2) |
| 向导血量 | 40 + 层×15 |
| 向导击杀奖励 | 金币 50+层×25，经验 80+层×40，史诗剑（攻+5+层）+史诗甲（防+3+层） |
| 商店 | 买 = 价值×2，卖 = 价值÷2 |
| 日志 | 容量 200，每屏 15，`历史` 全量 |
| 种子 | `./mud_text 42` 可复现地图 |

怪物属性按当前层数成长，例如哥布林 `HP 10+2L、攻 3+L`，魔王 `HP 60+10L、攻 12+L`。数值设计的基本思路是"玩家每层都有压力，但升级和装备能把压力压回去"。

## 10. 内存管理

项目使用大量动态内存（怪物、NPC、物品、地图、商店均为 `new` 分配），内存管理是重点处理的一块：

- `GameContext::cleanup()`：释放全部动态内存，可安全重复调用
- `clearLevelEntities()`：换层时只释放本层实体（怪物/NPC/物品/地图），同时清空向导血量表
- `~Player()`：析构时释放背包中的全部物品，避免泄漏
- 击杀怪物、移除向导、卖出装备时，都先 `delete` 再从容器中移除，避免悬空指针

整个程序运行结束（正常通关或死亡退出）后，在 `run()` 末尾调用 `cleanup()` 统一释放所有内存。

## 11. 遇到的问题与解决

**Q1：Windows 下双击 exe 没反应 / 一闪而过？**
这是控制台程序。先开 cmd 或 Windows Terminal，`cd` 到 exe 目录，输入 `mud_text` 回车运行。

**Q2：中文乱码 / 方块？**
Win10+ 用 Windows Terminal 或 cmd 直接运行（程序会自动切 UTF-8）。老终端（Win7 及以前）不支持 UTF-8 中文，需要换 Win10+ 或安装支持中文的终端。

**Q3：颜色不显示，显示 `←[91m` 乱码？**
程序启动会打开 `ENABLE_VIRTUAL_TERMINAL_PROCESSING`，Win10+ cmd / Windows Terminal 正常。极老终端不支持 ANSI，颜色码会原样显示，属正常降级。

**Q4：`买 1` 解析失败（乱码目标名）？**
这是曾修复过的一个 bug：中文参数不能按固定字节数切，`argAfter` 按 token 实际字节裁剪后解决。若仍有问题，需确认编译时 `/utf-8` 生效（MSVC 自动添加）。

**Q5：怎么固定地图方便测试？**
`./mud_text 42`——同一数字种子生成完全一致的地牢，方便回归测试。

## 12. 实验总结

本项目完整覆盖了 C++ 的几块核心内容：类与继承（`Entity` 派生 `Player / Monster / Npc / Item`）、虚函数与多态（`getDescription()` 纯虚接口）、STL 容器（`vector` 管理实体列表、`map` 管理向导血量表）、动态内存管理、CMake 工程组织，以及跨平台的中文编码处理。

印象较深的是两个"看不见的坑"：一是 UTF-8 中文按字节切参数会切出半个汉字，二是 Windows 控制台代码页与 ANSI 彩色开关。这两个问题表面是"乱码"，实质都是对字符编码和终端协议理解不透彻所致。解决这些问题的过程，加深了对"文本在计算机中如何存储"的具体认识，这是仅靠看书难以获得的经验。

后续可能的改进方向：将 `renderRoomDesc` 与 `logRoomDescription` 重复的出口描述抽成公共函数；为 `CombatSystem` 和 `DungeonGenerator` 补充单元测试；进一步丰富每层向导的剧情内容。

---

*文档完。*
